import styled from 'styled-components';

const StyledRoot = styled.section`
  width: 100%;
  height: 100%;
  position: relative;
`;

const SkyImg = styled.img``;

const SchoolImg = styled.img`
  position: absolute;
  left: 0;
  top: 0;
`;

const WeatherImg = styled.img`
  position: absolute;
  left: 0;
  top: 0;
`;

const BearImg = styled.img`
  position: absolute;
  left: 0;
  top: 0;
`;

export { StyledRoot, SkyImg, SchoolImg, WeatherImg, BearImg };
